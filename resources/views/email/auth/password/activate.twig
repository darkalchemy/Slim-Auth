{% set link = full_url_for('auth.activate', {}, {'email': user.email, 'code': code}) %}

<p>
    {{ __f('Hi') }} {{ user.username }},
</p>

<p>
    {{ __f('Click the link below to confirm your email and activate your account.') }}
</p>
<p>
    <a href="{{ link }}">{{ link }}</a>
</p>

<p>
    {{ settings.site_name }} {{ __f('Staff') }}
</p>
